import { IMessage } from "./formatter/interface";
interface Parameters {
    sql: string;
    host?: string;
    user?: string;
    port?: number;
    database?: string;
    driver?: string;
    prefix?: string;
    password?: string;
    verbosity?: number;
}
declare const _default: ({ sql, host, port, database, user, prefix, password, verbosity, driver, }: Parameters) => Promise<IMessage[]>;
export default _default;
